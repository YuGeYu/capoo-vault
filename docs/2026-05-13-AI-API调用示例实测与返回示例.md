# 2026-05-13 AI API 调用示例实测与返回示例

## 本次变更

- 使用临时 API Key 实际调用线上 `/api/open/ai-chat`。
- API 页面新增：
  - Python 调用示例
  - 返回示例
- 开放 API 响应移除了 `model` 字段，避免向用户暴露内部模型路由。
- 实测完成后，临时测试 Key 已禁用。

## 实测结果

- 请求地址：`https://maomaochongmiao.600318.xyz/api/open/ai-chat`
- 请求方式：`POST`
- 实测状态码：`200`
- 部署后实测返回对象不包含 `model` 字段。

## 部署记录

- 已执行 `npm run deploy`
- 部署版本：`5b58d149-d5f9-4632-b1ae-f15454b5ae0e`

## 验证

- `node --check src/worker.js`
- `node --check public/ai-api-tool.js`
- `node --check public/js/app-renderers.js`
- `node --check public/js/app-actions.js`

