# 2026-05-13 站长特供 AI API Key 开发记录

## 本次变更

- 新增站长特供 API Key 逻辑。
- 站长账号名下的 active API Key 调用 `/api/open/ai-chat` 时，自动识别为无限调用 Key。
- 站长特供 Key 不受每日免费次数限制。
- 站长特供 Key 不做余额不足拦截。
- 站长特供 Key 调用成功不扣费。
- 调用成功后仍会记录成功次数和 `last_used_at`，方便后台追踪。

## 响应标记

站长特供 Key 调用成功时，`usage` 中会返回：

```json
{
  "charged": false,
  "cost": "0.000",
  "todayFreeLimit": null,
  "membershipActive": true,
  "pricePerCall": "0.000",
  "unlimited": true
}
```

## 线上 Key

- 所属账号：站长账号 `18860966721`
- Key 前缀：`mmc_owner_698539`
- 备注：`站长特供无限调用 Key`
- 状态：`active`

完整 Key 只在聊天窗口中交付一次，不写入文档。

## 部署记录

已执行：

```bash
npm run deploy
```

部署版本：

```text
87d3a176-8ad1-4e27-9fa5-643931e126e5
```

## 实测

使用站长特供 Key 调用线上接口：

```http
POST https://maomaochongmiao.600318.xyz/api/open/ai-chat
```

实测状态码：

```text
200
```

实测返回包含：

- `charged: false`
- `cost: "0.000"`
- `todayFreeLimit: null`
- `pricePerCall: "0.000"`
- `unlimited: true`

